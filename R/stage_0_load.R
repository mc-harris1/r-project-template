# Replace this stage with input acquisition for the project.

log_message("Stage 0: loading input.")

# Set this to FALSE when no new input is available. The default keeps the
# template runnable without requiring a project-specific data source.
stage0_updated <- TRUE

log_message("Stage 0: input load complete.")
